#include "main.h"

/**
 * _strpbrk - searches a string for any of a set of bytes
 * @s: input string
 * @accept: char to be searched for in the input string
 * Return: 0
 */

char *_strpbrk(char *s, char *accept)
{
	return (0);
}
